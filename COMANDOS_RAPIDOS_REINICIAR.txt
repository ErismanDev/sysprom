# ==========================================
# COMANDOS RÁPIDOS - COPIAR E COLAR
# ==========================================

# Verificar processos gunicorn
ps aux | grep gunicorn

# Verificar serviços
sudo systemctl list-units | grep -i gunicorn
sudo systemctl list-units | grep -i seprom

# Verificar supervisor
sudo supervisorctl status

# Se encontrar um serviço, substitua NOME_DO_SERVICO:
# sudo systemctl restart NOME_DO_SERVICO

# Reiniciar Nginx (pode ser necessário)
sudo systemctl restart nginx

# Verificar se está funcionando
curl -I http://localhost:8000

# Se os arquivos foram coletados, apenas limpar cache do navegador pode funcionar
# (Ctrl+Shift+R ou Ctrl+F5)

