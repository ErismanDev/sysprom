# COMANDOS PARA ATUALIZAR SERVIDOR DIGITAL OCEAN
# Copie e cole no terminal do Cursor (PowerShell)

# Opção 1: Executar script
.\atualizar_servidor_do.ps1

# Opção 2: Comandos diretos (copie tudo de uma vez)
ssh root@64.23.185.235 "cd /home/seprom/sepromcbmepi && echo '📦 Atualizando...' && source venv/bin/activate && pip install --upgrade pip --quiet && pip install -r requirements_production.txt --quiet && echo '🗄️  Migrations...' && python manage.py migrate --noinput && echo '📁 Estáticos...' && python manage.py collectstatic --noinput --clear && echo '🔄 Reiniciando...' && sudo systemctl restart seprom && sleep 2 && echo '✅ Concluído!' && sudo systemctl status seprom --no-pager -l | head -15"

# Opção 3: Passo a passo (se preferir executar um por vez)
ssh root@64.23.185.235
cd /home/seprom/sepromcbmepi
source venv/bin/activate
pip install --upgrade pip
pip install -r requirements_production.txt
python manage.py migrate --noinput
python manage.py collectstatic --noinput --clear
sudo systemctl restart seprom
sudo systemctl status seprom

