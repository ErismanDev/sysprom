===============================================================
🚀 COMANDOS PARA EXECUTAR NO SERVIDOR AGORA
===============================================================

Você está conectado ao servidor. Execute estes comandos:

1. Ir para o diretório do projeto:
cd /home/seprom/sepromcbmepi

2. Ativar ambiente virtual:
source venv/bin/activate

3. Adicionar campos no banco de dados:
sudo -u postgres psql -d sepromcbmepi -c "ALTER TABLE militares_armaparticular ADD COLUMN IF NOT EXISTS alma_raiada BOOLEAN DEFAULT FALSE;"
sudo -u postgres psql -d sepromcbmepi -c "ALTER TABLE militares_armaparticular ADD COLUMN IF NOT EXISTS quantidade_raias INTEGER NULL;"
sudo -u postgres psql -d sepromcbmepi -c "ALTER TABLE militares_armaparticular ADD COLUMN IF NOT EXISTS direcao_raias VARCHAR(10) NULL;"

4. Coletar arquivos estáticos:
python manage.py collectstatic --noinput --clear

5. Reiniciar serviço:
sudo systemctl restart seprom

6. Verificar status:
sudo systemctl status seprom

===============================================================
OU EXECUTE TUDO DE UMA VEZ:
===============================================================

cd /home/seprom/sepromcbmepi && source venv/bin/activate && sudo -u postgres psql -d sepromcbmepi -c "ALTER TABLE militares_armaparticular ADD COLUMN IF NOT EXISTS alma_raiada BOOLEAN DEFAULT FALSE;" && sudo -u postgres psql -d sepromcbmepi -c "ALTER TABLE militares_armaparticular ADD COLUMN IF NOT EXISTS quantidade_raias INTEGER NULL;" && sudo -u postgres psql -d sepromcbmepi -c "ALTER TABLE militares_armaparticular ADD COLUMN IF NOT EXISTS direcao_raias VARCHAR(10) NULL;" && python manage.py collectstatic --noinput --clear && sudo systemctl restart seprom && echo "✅ DEPLOY CONCLUÍDO!"
